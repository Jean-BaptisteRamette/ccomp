;; test program: source.c8

;; method 1:
;;      store token ".done" and its scope N until we find it
;;      if it was not defined in the scope N - 1 then throw an error
;;
;;
;; TODO: store label symbol until we find it
;;       when we leave the function and undefined_labels is not empty -> undefined error
;;       when there is no more statements and undefined_labels is not empty -> undefined error
;;

proc scope2
.start:
    jmp .somewhere
    ret
endp scope1

proc scope1
    jmp .somewhere
    ret
endp scope1


.main:
    jmp .somewhere // scope level 1


.exit:
    xor r1, r1

